RRF-based-models

This repository provides Python implementations of RRF-based calibration models for back-calculating concentrations in LC–MS quantitative analysis.
1. Environment
Python >= 3.8
Required packages:
pandas
numpy
matplotlib
2. Input Format
Input data are provided as an Excel file (.xlsx).
The required columns and data structure are demonstrated in the example file included in this repository.
